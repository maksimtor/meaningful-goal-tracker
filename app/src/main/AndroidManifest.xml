<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.maksimtoropygin.meaningfulgoaltracker">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_new"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_new_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <receiver android:name=".Receiver"></receiver>

        <activity android:name=".ui.value_explorer.ValueExplorerAddEdit"></activity>
        <activity android:name=".ui.goal_keeper.GoalKeeperAddEdit"></activity>
        <activity android:name=".ui.todo_list.ToDoAddEdit"></activity>
        <activity android:name=".ui.scheduler.SchedulerAddEdit"></activity>
        <activity
            android:name=".auth.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".auth.RegisterActivity"
            android:label="@string/title_activity_register" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>